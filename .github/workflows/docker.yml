name: docker
on:
  push:
    branches:
    - master

jobs:
  docker:        
    name: unit
    runs-on: self-hosted

    steps:
    - id: checkout
      uses: actions/checkout@v2

    - id: publish
      uses: docker/build-push-action@v1
      with:
        username: tam7t
        password: hunter2
        repository: tam7t/dogecho
        tags: latest
